{
  "openapi": "3.0.3",
  "info": {
    "title": "Echo Agent Platform API",
    "version": "1.0.0",
    "description": "Autonomous AI Agent Platform runtime, mission execution engine, skills, features, and model proxy API.\n\nTriggers autonomous agent execution loops using configurable LLM providers (OpenAI, Anthropic, LM Studio, OpenCode Go) with full tool-calling, memory, and streaming support."
  },
  "servers": [
    {
      "url": "/",
      "description": "Current Environment Server"
    }
  ],
  "security": [
    {
      "InternalTokenAuth": []
    }
  ],
  "components": {
    "securitySchemes": {
      "InternalTokenAuth": {
        "type": "apiKey",
        "in": "header",
        "name": "X-Internal-Token",
        "description": "Internal service-to-service authentication token"
      },
      "BearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT",
        "description": "User JWT Bearer token for authenticated API access"
      }
    },
    "schemas": {
      "Feature": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "example": "web_search"
          },
          "name": {
            "type": "string",
            "example": "web_search"
          },
          "description": {
            "type": "string",
            "example": "Web search engine for real-time weather, prices, and current events."
          }
        },
        "required": ["id", "name", "description"]
      }
    }
  },
  "paths": {
    "/api/v1/features": {
      "get": {
        "summary": "List implemented agent features",
        "description": "Fetch the implemented tool registry — the feature IDs the agent can actually execute, dynamically derived from the tool registry. Each entry maps a requestable feature id to its loaded tool name and description. Catalog metadata (tier_requirement, ui_schema, status) is owned by the backend features table, not the agent.",
        "tags": ["Features"],
        "security": [
          {
            "InternalTokenAuth": []
          },
          {
            "BearerAuth": []
          }
        ],
        "responses": {
          "200": {
            "description": "Implemented tool registry",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/Feature"
                  }
                },
                "example": [
                  {
                    "id": "delegate_task",
                    "name": "delegate_task",
                    "description": "Delegate a specific sub-task or research query to a specialized child/sub-agent. This isolates the parent workspace and keeps the context window clean."
                  },
                  {
                    "id": "web_search",
                    "name": "web_search",
                    "description": "Web search engine for real-time weather, prices, and current events."
                  },
                  {
                    "id": "write_todos",
                    "name": "write_todos",
                    "description": "Create, update, or reorganize the agent's task plan (todo list). Use this tool to set or modify the planned execution steps."
                  }
                ]
              }
            }
          }
        }
      }
    }
  }
}
