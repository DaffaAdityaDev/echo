export const MODEL_CONSTANTS = {
  MODELS_PATH: '/models',
} as const;

export const LOG_MESSAGES = {
  FETCHING_MODELS: 'Fetching models from provider',
  FETCH_FAILED: 'Failed to fetch models',
} as const;
